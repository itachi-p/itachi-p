> summary: Codeタブ破棄・CLI運用への切り替え方針確定／Gitブランチ乖離の修正／タイマーUI要改善（直接修正予定）

# CLI運用への切り替えとGit修正

## 日付
2026-05-04

## 実施内容

### 1. Gitブランチ乖離の修正
- Codeタブがgit pullせずにローカルcommitしたためローカルとリモートが diverged
- `git stash → git pull --rebase origin main → git stash pop`で解決
- 競合なし、ローカルがリモートより1commit先行の状態に

### 2. 運用方針の切り替え確定
- デスクトップアプリCodeタブ → 破棄
- CLI（Warp + Claude Code）+ このChat → 採用
- 役割分担：実装はCLI、方針・判断・docs生成はChat（以後デスクトップアプリのCodeタブは基本不使用）

## 判断

| 項目 | 判断 | 根拠 |
|---|---|---|
| Codeタブ | 破棄 | 反応遅延・憲法違反・改悪・クラッシュ誘発。2日間の検証で十分な根拠が揃った |
| CLI中心への切り替え | 採用 | Warpとの組み合わせでエラー修正ループが速い。ターミナル出力をリアルタイムで読める |
| timer_app.pyクラッシュ修正 | 未完了 | 深夜のため持ち越し |

## 気づき

- CodeタブはCLIと同等の文脈把握ができるという前提が誤りだった（2日で確認）
- Gitの乖離はCodeタブが自律的にcommitしたことで発生。AIエージェントへの権限委譲リスクの具体例
- `git stash → rebase → stash pop`パターンをここで初めて使用

## 未完了・次回持ち越し

- timer_app.pyのクラッシュ修正（git restore または直接編集）
- push完了（現在ローカルが1commit先行）
- 5/4分docsの追記（疑似スモールビジネス課題着手予定）
- `2026-04-26_preference-extraction_01.md`の圧縮サマリ追記

## 何が間違っていたか（自戒ログ）

- Codeタブを「入門用として試す」判断自体は正しかったが、CLAUDE.mdの憲法が守られるかを最初の1操作で検証すべきだった。2日間かけて確認したのは非効率
