# ツール選択の判断記録 - Claude Chat vs Claude Code

[2026-05-14_borderless-application-strategy.md](./2026-05-14_borderless-application-strategy.md)の壁打ち中に派生したメタ判断の記録。応募戦略本体とは独立したプロジェクト運用上の判断として分離して記録する。

---

## 目的設定

プロジェクトの記録ツール選択を最適化する。具体的には、Claude Pro課金中(残り10日強、5/26まで)に放置状態だったClaude Codeを使うべきか、現状のClaude Chatのみで進めるべきかを判断する。

ツール選択そのものをサイクル化し、毎回「今日はどっち?」と判断する負荷を消すことが目的。

---

## 仮説:フェーズによって最適ツールが変わる

ChatとCodeは構造的特性が異なるため、現プロジェクトのフェーズによって最適ツールが切り替わるという仮説。

**Claude Chatの特性**
- 会話の継続性が中心、思考の往復が主目的
- メモリ機能で過去の文脈が引き継がれる
- ファイル生成はダウンロードを介する
- 思考の壁打ち・構造化に最適

**Claude Codeの特性**
- ローカルファイルシステムへの直接読み書き
- git操作との統合(commit/pushまで自動化可能)
- 複数ファイルの横断的な参照・編集に強い
- 思考よりも実装・編集タスクに最適

---

## 判断:If-then方式でのフェーズ別ルール

迷いをなくして素早く行動するためのIf-thenルールとして以下を採用する。

- **If 壁打ち・思考の往復が中心 → then Claude Chat**
- **If docs以下のマークダウン群を横断参照しながら新規docs作成 → then Claude Code**
- **If 既存docsを少しずつ書き換えて全体を統合する作業 → then Claude Code**
- **If 単発のdocs生成 + 並行する思考の整理 → then Claude Chat**

このルールにより、毎回の判断オーバーヘッドが消え、思考リソースを本来の対象(事業仮説・応募戦略)に集中できる。

---

## 適用フレームワーク

### If-then方式

判断を事前にルール化することで、行動時の迷いを排除する。ツール選択もこの方式に乗せる対象。

### サンクコストの警戒

「課金してるから使わなきゃ」で意思決定が歪むことを警戒する。$22という具体的金額に対しても冷静に「本末転倒」と判定する。Pro課金は手段であって、使用率を上げることが目的ではない。

### 最先端追従の機会費用

ツールは半年〜1年で標準化されるか、より良いものに置き換わる。先行コストを払う価値は「半年早く使える便益」が「学習コスト+陳腐化リスク」を上回る場合に限られる。情報商材的なポジショントークはここを意図的に隠して煽る構造になっている。

---

## Pro期間中の実験設計

5/26までの残り期間で、ツール選択ルールを実地検証する。

- **〜5/19頃:Chatのまま継続**(現在のフェーズが壁打ち中心のため)
- **5/20頃:Codeで1日試す**(5/14と5/15のdocsを横断参照しながら新規docを書く実験)
- **5/26まで:両方使った経験から、Pro継続判断の材料にする**

この期間設計により、ツール選択も検証可能なサイクルに乗せる。

---

## 現フェーズの判断

応募戦略の構造化と判断が中心の現フェーズでは**Claude Chatが優位**と判断する。理由:対話の深さが価値の源泉になっているフェーズだから。

ただし、5/16以降のフェーズ(調査・事業仮説詰め)では横断参照の必要性が高まる可能性があり、その時点でCodeへの切り替えを検討する。

---

## メタ的観察

- ツール選択そのものを記録対象にすることで、プロジェクト運用の再現可能なパターン確立に寄与する
- 「最先端ツールを我先に使う」というプレッシャーから自由になることで、本来の目的に集中できる
- ツールは半年で陳腐化する前提を持つことで、特定ツールへの依存リスクを低減する
- サンクコストの呪縛は具体的金額(月額$22)に対しても発生しうる。明示的に警戒対象にする

---

## Mermaid記法の運用方針(2026-05-18追記)

### Mermaidとは

グラフ構造をテキストで記述するDSL(ドメイン固有言語)。マークダウンファイルの` ```mermaid〜``` `ブロック内に書くと、GitHub・VSCode・Obsidianなどが自動的に図としてレンダリングする。PNGを別管理せずdocsに図を埋め込める。

### If-then 使い分けルール

- **If ATT(ambitious-target-trees)を生成する → then Mermaid(flowchart TD)を使う**
  - 木構造との相性が良く、docsに直接埋め込める
  - GitHubで自動レンダリングされるのでPNG管理が不要

- **If マインドマップを生成する → then 状況による**
  - 簡易な階層構造ならMermaid(mindmap記法)で試みる
  - 複雑な放射状レイアウトが必要ならPNG静画のまま

- **If TOCfEのクラウド図を生成する → then PNG静画を維持する**
  - 左右対称の特殊構造がMermaidでは自然に表現できない
  - flowchart LRで代替表現は可能だが完全再現は難しく、コストに見合わない

- **If ロジックブランチを生成する → then Mermaid(flowchart TD)を試みる**
  - 因果関係の木構造はMermaidと相性が良い

### Mermaidの主要記法(参照用)

```
flowchart TD   // 上から下への階層図(ATT、ロジックブランチ向き)
flowchart LR   // 左から右への流れ図
mindmap        // 放射状のマインドマップ
sequenceDiagram // シーケンス図
gantt          // ガントチャート
```

### メタ原則

「ツールの価値は文脈依存」と同様に、Mermaidも万能ではない。図の種類とツールの相性で判断する。テキストとして管理できる図はMermaid化を優先するが、無理に使わない。

---

## 次の検証ポイント

5/20頃のCode実験後に追記する内容:

- Codeを使った結果、横断参照の効率はChatと比べてどう変わったか
- gitコミット・プッシュまでの一連の流れが効率化されたか
- 思考の深さが犠牲になっていないか
- If-thenルールの粒度は適切だったか、修正が必要か

これらの観察を5/20以降のdocsに記録し、Pro継続判断の材料にする。
